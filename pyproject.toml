[tool.poetry]
name = "repl_python3_SandPortal"
version = "0.1.0"
description = ""
authors = ["Cypher3d <<>>"]

[tool.poetry.dependencies]
python = "^3.8"
django = "^3.2"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"
